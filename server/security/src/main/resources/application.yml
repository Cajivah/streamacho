spring:
  datasource:
    url: jdbc:postgresql://localhost:55432/streamacho-security
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
